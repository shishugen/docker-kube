<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.kube.dao.clazz.KubeClassStudentsDao">
	
	<!-- 查询数据
	<select id="findList" resultType="KubeClassStudents">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->

	SELECT * FROM `kube_class_students` c WHERE c.class_id = '1208624803403313152'  AND c.user_id  NOT IN(
	SELECT a.`user_id` FROM `kube_vm` a WHERE a.`apply_id` = '1209388506417901568' AND  a.`user_id` IS NOT NULL
	)

	<select id="findApplyIdNotBind" resultType="KubeClassStudents">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
        LEFT JOIN `kube_vm` v ON v.user_id = a.user_id
        <where>
				v.`apply_id` = #{applyId}
			AND	v.`user_id`  IS NULL
			AND v.`status` = 0
		</where>
	</select>
</mapper>